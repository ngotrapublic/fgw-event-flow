# Dependencies
node_modules/
client/node_modules/
server/node_modules/

# Environment variables
# Chỉ ignore file .env gốc (chứa secrets), KHÔNG ignore .env.production
.env
server/.env
# .env.production files are SAFE to commit (chỉ chứa API URL, không có secrets)

# IDE
.vscode/
.idea/

# Build output
client/dist/
client/build/

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Firebase Admin SDK (if downloaded)
server/serviceAccountKey.json

# Old backup files
server/*.backup.js
server/*.old.js
server/*.temp.js

# Old JSON data files (migrated to Firestore)
server/data/*.json
server/data/*.backup.json
